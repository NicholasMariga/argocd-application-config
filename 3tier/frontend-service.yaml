apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: 3tier
spec:
  selector:
    app: nginx-frontend
  ports:
  - protocol: TCP
    port: 9091        # ← external port you access in browser
    targetPort: 80    # ← nginx still listens on 80 internally
  type: NodePort